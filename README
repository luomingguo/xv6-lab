xv6 is a re-implementation of Dennis Ritchie's and Ken Thompson's Unix
Version 6 (v6).  xv6 loosely follows the structure and style of v6,
but is implemented for a modern RISC-V multiprocessor using ANSI C.

ACKNOWLEDGMENTS

xv6 is inspired by John Lions's Commentary on UNIX 6th Edition (Peer
to Peer Communications; ISBN: 1-57398-013-7; 1st edition (June 14,
2000)).  See also https://pdos.csail.mit.edu/6.1810/, which provides
pointers to on-line resources for v6.

BUILDING AND RUNNING XV6

You will need a RISC-V "newlib" tool chain from
https://github.com/riscv/riscv-gnu-toolchain, and qemu compiled for
riscv64-softmmu.  Once they are installed, and in your shell
search path, you can run "make qemu".

安装教程： https://pdos.csail.mit.edu/6.1810/2025/tools.html

# 关于调试
## 退出终端
ctrl + a 释放后按 x


## 利用打印调试
可以用 script 命令记录控制台输出，运行：make qemu 里面包裹在 script 环境下，这样输出会被保存到文件中方便搜索， 最后记得退出，键入exit或者logout或者control-D

script的用法： script [file [command ...]] 


## GDB
1. 一个窗口运行 make qemu-gdb
2. 另一个窗口运行 gdb-multiarch（或 riscv64-linux-gnu-gdb）
3. 设置断点 `b panic`
4. 输入c继续运行
5. 用bt查看调用栈

## 查看编译生成的汇编文件
编译 xv6 时，Makefile 会生成：
- kernel/kernel.asm：内核对应的汇编
- 每个用户程序也会生成 .asm 文件，用来分析特定地址对应的指令或函数。

## 调试内核崩溃或死锁
对于崩溃问题
- 内核会打印 sepc（程序计数器）值
- 可以在 kernel.asm 中查这个地址对应的函数- 或者运行 `addr2line -e kernel/kernel pc-value` 查看对应的源码行

对于卡住问题
- 用GDB， 当系统卡住时按 Ctrl+C，bt看堆栈

## QEMU 调试监控台
按下 Ctrl-a c 进入 QEMU monitor（调试控制台）。
常用命令：
- info mem: 查看内存页表
- cpu：切换查看不同核的状态
或者直接单核启动 `make CPUS=1 qemu`